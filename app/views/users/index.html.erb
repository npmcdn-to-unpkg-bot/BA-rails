<%= render 'shared/page_title', title: "All Users" %>

<!-- <ul class="nav nav-tabs">
    <li>
      <%= link_to "Posts", posts_path %>
    </li>
    <li class="active">
      <%= link_to "Users", users_path %>
    </li>
</ul> -->


    <%= will_paginate @user %>


<% @user.each do |user| %>
        <div class="col-md-4 posts-text">
            <a href="/users/<%= user.id%>">
              <span class="text-user"> <%= user.username %></span>
                <%= image_tag(user.picture.url, class: "img-circle", size: "250x250") %>
                </a>
          </div>
  <% end %>
